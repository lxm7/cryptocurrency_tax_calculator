[project]
name = "webhooks"
version = "0.1.0"
description = "Stripe webhook receiver (Flask)"
requires-python = ">=3.13"
dependencies = [
    "flask>=3",
    "gunicorn>=22",
]

[tool.uv]
package = false
